# SAIGBOX V3 Environment Variables

# OAuth Application Credentials
# These are YOUR app's credentials from Google Cloud Console / Azure Portal
# Users will authenticate through OAuth and provide their own access to their emails

# Google OAuth (Create app at https://console.cloud.google.com/)
GMAIL_CLIENT_ID=your_google_oauth_app_client_id
GMAIL_CLIENT_SECRET=your_google_oauth_app_client_secret
GMAIL_REDIRECT_URI=http://localhost:8000/api/auth/google/callback

# Microsoft OAuth (Create app at https://portal.azure.com/)
MICROSOFT_CLIENT_ID=your_microsoft_app_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_app_client_secret
MICROSOFT_REDIRECT_URI=http://localhost:8000/api/auth/microsoft/callback

# JWT Configuration for Session Management
SECRET_KEY=generate_a_secure_random_string_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=30

# Anthropic API for SAIG AI Assistant
ANTHROPIC_API_KEY=your_anthropic_api_key

# AWS Bedrock for SAIG AI Assistant (Alternative to Anthropic)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# Urgency Detection Settings
URGENCY_THRESHOLD=40  # Score threshold for marking urgent (0-100)
ACTION_CONFIDENCE_THRESHOLD=70  # Min confidence for auto-creating actions
URGENCY_BATCH_SIZE=5  # Number of urgent emails to process at once
URGENCY_PROCESSING_INTERVAL=30  # Seconds between batch processing
ENABLE_AUTO_ACTIONS=true  # Master switch for automatic action creation

# Database
DATABASE_URL=sqlite:///saigbox.db

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Environment
ENVIRONMENT=development

# IMPORTANT NOTES:
# 1. The GMAIL_CLIENT_ID and GMAIL_CLIENT_SECRET are for YOUR OAuth app, not user credentials
# 2. Users will grant access to their emails during the OAuth flow
# 3. User email credentials are NEVER stored in environment variables
# 4. Each user's OAuth tokens are stored securely in the database after authentication